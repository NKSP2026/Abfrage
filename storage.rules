rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /ehsi_sources/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.auth.uid == 'SrRHH7RgmDRXOCKcHy2mOQANrTk1';
    }
  }
}
